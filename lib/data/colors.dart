// Light mode colors
import 'package:flutter/material.dart';

const lightBackground = Colors.white;
const lightBackgroundCard = Color(0xfff9f4fb);
const lightText = Colors.black;
const lightTextSecondary = Color(0xff4e4e4e);
const lightAccent = Color(0xFF4a3898);
const lightAccentSecondary = Color(0xff5933ac);

// Dark mode colors
const darkBackground = Colors.black;
const darkBackgroundCard = Color(0xff222222); // Darker shade for cards
const darkText = Colors.white;
const darkTextSecondary =  Color(0xffbbbbbb); // Lighter gray for secondary text
const darkAccent =  Color(0xFF8c7eff); // Adjusted for dark mode
const darkAccentSecondary =  Color(0xFFa998ff); // Adjusted for dark mode
